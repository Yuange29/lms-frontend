import { CourseWrapper, CoursesWrapper } from "./courses-style";

import styled from "styled-components";

export const QuizWrapper = styled(CoursesWrapper)``;

export const CourseCardWrapper = styled(CourseWrapper)`
    .indent {
        text-indent: 1em;
    }
`;
