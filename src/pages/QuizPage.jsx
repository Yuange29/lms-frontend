import { useState } from "react";

export default function QuizPage() {
    const [quizId, setQuizId] = useState(null);

    console.log(quizId);

    return (
        <>
            <div>hello</div>
        </>
    );
}
